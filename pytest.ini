[pytest]
testpaths = tests
# tests/ sits beside the modules it imports; conftest.py puts the project root
# on sys.path so `import main` works without packaging the app.
pythonpath = . tests
addopts = -q --strict-markers
